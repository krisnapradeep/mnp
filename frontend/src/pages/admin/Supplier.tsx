import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const Supplier: React.FC = () => {
    return (
        <Box p={5}>
            <Heading as="h2">Supplier</Heading>
            {/* Add your content here */}
        </Box>
    );
};

export default Supplier;
